function nextletter(str){
    for(let a=0; a<str.length; a++){
        if(96< str,charCodeAt(a)&& str.charCodeAt< 123){
            str=str.replace(str.charAt(a),
            String.fromCharCode(str.charCodeAt(a)+1));
        }
    }
    var str= 'hello'
    return str
}

